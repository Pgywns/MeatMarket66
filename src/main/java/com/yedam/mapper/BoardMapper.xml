<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.mapper.BoardMapper">
<!-- 전체조회 -->
<select id="selectList" resultType="Board">
select board_no,
       user_id,
       board_title,
       board_content,
       board_date,
       board_category,
       answer
from tbl_board
</select>
	<!-- 1:1문의 
	<insert id="insertcsBord">
		insert into tbl_board(b_no,u_id,b_title,b_content,b_category)
		values(#{bNo},#{uId},#{bTitle},#{bContent},#{bContent},#{bCategory})
	</insert>-->
<select id="clickList" resultType="Board" parameterType="String">
select board_no,
       user_id,
       board_title,
       board_content,
       board_date,
       board_category,
       answer
from tbl_board
where board_category = #{boardCategory}
</select>
</mapper>