<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.mapper.ProductMapper">

	<!-- 상품 전체 -->
	<select id="selectList" resultType="Product">
		select *
		from tbl_product
		order by prd_no
	</select>
	
	<!-- 상품 카테고리별 수량 -->
	<select id="selectCount" resultType="Product">
		select count(prd_sort) "prdCount", p_sort "prdSort" 
		from tbl_product
		group by p_sort
	</select>
	
	<!-- 상품 목록 카테고리 -->
	<select id="selectCategory">
		select * from tbl_product
		where p_sort = ${pSort}
	</select>

	<!-- 상품목록 페이징 -->
	<select id="selectListPaging">
		SELECT a.*
		from (SELECT p_no pn, r.*
		FROM TBL_PRODUCT r
		order by p_no) a
		where a.pn > (${page} - 1 ) * 9 * 0
		<![CDATA[
		and a.pn <= (${page} * 9)
		]]>

	</select>
</mapper>